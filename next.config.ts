import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['1.bp.blogspot.com', 'love.doghero.com.br', 'aloalobahia.com'], // Adicione o domínio das imagens
  },
};


export default nextConfig;
